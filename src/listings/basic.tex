\section{Basic Listings}

\subsection{Using the \code{lstlisting} environment}

  \begin{lstlisting}[
      style=nonumbers,
      style=colored,
  ]
def greet(name):
    print("Hello " + name + "!")
    \end{lstlisting}

\subsection{Using \code{\\lstinputlisting}}
  \lstset{inputpath=code}
  
  \lstinputlisting[
      style=numbers,
      style=colored,
      caption={The sieve of Eratosthenes in Python.},
      label={lst:basic}
  ]{sieve.py}
